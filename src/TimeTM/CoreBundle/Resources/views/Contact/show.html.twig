
{% block mainContent -%}

<div class="listContainer">

    <h1>{{ 'contact.details'|trans }}</h1>

    <table class="view overflow">
        <tbody>
            <tr>
                <td>
                    {{ 'contact.lastname'|trans }}
                </td>
                <td>
                    {{ entity.lastname }}
                </td>
            </tr>
            <tr>
                <td>
                    {{ 'contact.firstname'|trans }}
                </td>
                <td>
                    {{ entity.firstname }}
                </td>
            </tr>
            <tr>
                <td>
                    {{ 'contact.email'|trans }}
                </td>
                <td>
                    {{ entity.email }}
                </td>
            </tr>
            <tr>
                <td>
                    {{ 'contact.phone'|trans }}
                </td>
                <td>
                    {{ entity.phone }}
                </td>
            </tr>
            <tr>
                <td>
                    {{ 'contact.company'|trans }}
                </td>
                <td>
                    {% if entity.isCompany %}
                        {{ 'val.yes'|trans }}
                    {% else %}
                        {{ 'val.no'|trans }}
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td>
                    {{ 'contact.client'|trans }}
                </td>
                <td>
                    {% if entity.isClient %}
                        {{ 'val.yes'|trans }}
                    {% else %}
                        {{ 'val.no'|trans }}
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="align-center" colspan="2">
					<a id="eventBackButton" class="button" href="{{ path('contact') }}">{{ buttonText |trans }}</a>
					<a class="button actionButton" href="{{ path('contact_edit', { 'id': entity.id }) }}">
					    {{ 'action.edit'|trans }}
					</a>
                </td>
            </tr>
        </tbody>
    </table>


</div>
{% endblock %}
