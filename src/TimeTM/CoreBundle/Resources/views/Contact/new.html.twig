{% extends '::base.html.twig' %}

{% block mainContent -%}
<div class="listContainer">
    <h1>Contact creation</h1>

    <div>{% if msg is defined %}{{ msg }}{% endif %}</div>
    
    
    {{ form(form) }}

        <ul class="record_actions">
		    <li>
		        <a href="{{ path('contact') }}">
		            Back to the list
		        </a>
		    </li>
		</ul>
</div>
{% endblock %}
