{% extends '::base.html.twig' %}

{% block title %}

    {% if template == 'index' %}
        {{ 'contact.name.plur'|trans|capitalize }}
    {% elseif template == 'new' %}
        {{ 'contact.new'|trans|capitalize }}
    {% elseif template == 'show' %}
        {{ 'contact.details'|trans|capitalize }}
    {% elseif template == 'edit' %}
        {{ 'contact.edit'|trans|capitalize }}
    {% endif %}

{% endblock %}

{% block ttm_stylesheets %}
	{{ parent() }}
	<link href="/css/calendarpanel.css" media="screen, projection" rel="stylesheet" type="text/css" />
{% endblock %}


{% block mainContent %}
<section id="ttm_calendarContainer">
	{% include 'TimeTMCoreBundle:Contact:container.html.twig' %}
</section>
{% endblock %}
